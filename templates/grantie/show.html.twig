{% extends 'base.html.twig' %}

{% block title %}Grantie{% endblock %}

{% block body %}
    <h1>Grantie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grantie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ grantie.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('grantie_index') }}">back to list</a>

    <a href="{{ path('grantie_edit', {'id': grantie.id}) }}">edit</a>

    {{ include('grantie/_delete_form.html.twig') }}
{% endblock %}
