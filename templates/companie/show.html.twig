{% extends 'base.html.twig' %}

{% block title %}Companie{% endblock %}

{% block body %}
    <h1>Companie</h1>
<body class="hold-transition sidebar-mini">
        <div class="content-wrapper">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ companie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ companie.nom }}</td>
            </tr>
            <tr>
                <th>DateFondation</th>
                <td>{{ companie.dateFondation ? companie.dateFondation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ companie.adresse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('companie_index') }}">back to list</a>

    <a href="{{ path('companie_edit', {'id': companie.id}) }}">edit</a>
</div>
    {{ include('companie/_delete_form.html.twig') }}
{% endblock %}
