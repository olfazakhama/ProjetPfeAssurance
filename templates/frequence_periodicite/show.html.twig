{% extends 'base.html.twig' %}

{% block title %}FrequencePeriodicite{% endblock %}

{% block body %}
    <h1>FrequencePeriodicite</h1>
<body class="hold-transition sidebar-mini">
        <div class="content-wrapper">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ frequence_periodicite.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ frequence_periodicite.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('frequence_periodicite_index') }}">back to list</a>

    <a href="{{ path('frequence_periodicite_edit', {'id': frequence_periodicite.id}) }}">edit</a>
</div>
    {{ include('frequence_periodicite/_delete_form.html.twig') }}
{% endblock %}
